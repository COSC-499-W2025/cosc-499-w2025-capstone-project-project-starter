# Example environment configuration for all features.
# Copy to .env and fill with your project-specific values.
# DO NOT COMMIT YOUR ACTUAL .env FILE.

# Supabase (required for auth, storage, resume syncing, config manager)
SUPABASE_URL=https://your-project.supabase.co          # Supabase project URL
SUPABASE_KEY=your-service-role-key                     # Service role key used by backend/CLI
SUPABASE_ANON_KEY=your-anon-key                        # Public anon key used for user sessions and JS uploads

# User-scoped scan preferences (optional; enables remote profile loading in parse_zip)
SCAN_USER_ID=00000000-0000-0000-0000-000000000000      # Only used by CLI/headless parse_zip to load a user's scan profile; TUI loads preferences from the logged-in session instead

# AI features (optional; required for LLMRemoteMediaAnalyzer and in-app AI analysis)
OPENAI_API_KEY=sk-...                                  # OpenAI API key; leave unset to disable AI calls

# CLI/Textual commit attribution filters (optional)
TEXTUAL_SKILL_PROGRESS_EMAILS=user@example.com         # Comma-separated emails to filter timelines
PORTFOLIO_USER_EMAIL=user@example.com                  # Preferred single email; wins if both are set

# Local Supabase tooling (optional)
SUPABASE_LOCAL=sbp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx   # Supabase CLI project ref if running supabase start

# Reserved/experimental
ENCRYPTION_MASTER_KEY=base64-encoded-key               # Not currently consumed; placeholder for secret encryption
